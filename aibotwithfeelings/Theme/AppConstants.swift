import Foundation

enum AppTheme {
    static let botName = "AIBotWithFeelings"

    enum Colors {
        static let userBubble = "UserBubble"
        static let botBubble = "BotBubble"
    }
}

enum StorageKey {
    static let userProfile = "aibwf.userProfile"
    static let appSettings = "aibwf.appSettings"
    static let chatHistory = "aibwf.chatHistory"
    static let emotionalMemories = "aibwf.emotionalMemories"
}
